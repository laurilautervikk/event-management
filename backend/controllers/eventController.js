export async function getData(req, res) {
  // try {
  //   const data = { message: "It's an event" };
  //   res.status(200).send(data);
  // } catch {
  //   res.status(404).send({ error: "No event found" });
  // }
}
